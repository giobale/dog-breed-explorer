# Core dlt framework with BigQuery support
dlt[bigquery]>=1.18.2,<2.0

# HTTP requests
requests==2.31.0

# Environment variables management
python-dotenv==1.0.0

# Google Cloud BigQuery client for integration testing
google-cloud-bigquery>=3.10.0
google-cloud-bigquery-storage>=2.20.0

# Testing dependencies (3.13-compatible)
pytest>=8.2
pytest-mock==3.12.0
responses==0.24.1
