# ==============================================================================
# DOG BREEDS DATA PLATFORM - ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env and fill in the actual values for your environment.
# The .env file is ignored by git and should never be committed.

# ------------------------------------------------------------------------------
# Google Cloud Platform Configuration
# ------------------------------------------------------------------------------
# GCP Project ID where resources will be deployed
GCP_PROJECT_ID=your-gcp-project-id

# GCP Region for resource deployment
GCP_REGION=us-central1

# Service Account Authentication (for pipeline execution)
# Path to service account JSON key file (required for BigQuery access)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account-key.json

# Alternative: Service Account Email (if using workload identity or other auth methods)
GCP_SERVICE_ACCOUNT_EMAIL=your-service-account@your-gcp-project-id.iam.gserviceaccount.com

# ------------------------------------------------------------------------------
# Dog API Configuration
# ------------------------------------------------------------------------------
# Base URL for The Dog API
DOG_API_BASE_URL=https://api.thedogapi.com/v1

# API endpoint for breeds data
DOG_API_ENDPOINT=breeds

# ------------------------------------------------------------------------------
# BigQuery Configuration
# ------------------------------------------------------------------------------
# BigQuery dataset name for raw dog breeds data
BIGQUERY_DATASET=dog_breeds_raw

# ------------------------------------------------------------------------------
# DLT Pipeline Configuration
# ------------------------------------------------------------------------------
# DLT pipeline logging level (DEBUG, INFO, WARNING, ERROR)
DLT_LOG_LEVEL=INFO

# DLT pipeline working directory (optional, defaults to .dlt in project root)
DLT_DATA_DIR=.dlt

# ------------------------------------------------------------------------------
# Local Development & Testing
# ------------------------------------------------------------------------------
# Environment identifier (dev, staging, production)
ENVIRONMENT=dev
